<h1>
  rooms create page
</h1>

<h2>
  Created Success!!
</h2>

<div>Click this
  <%= form_with url: '/choose_preset' do |form| %>
    <%= form.button 'button', value: @create_room_player_id, name: 'create_room_player_id' %>
    <%= form.hidden_field :string, value: @room_id.to_s, name: 'room_id' %>
    <%= form.hidden_field :string, value: 'red_hand', name: 'which_hand' %>
    <%= form.hidden_field :string, value: @current_player_id, name: 'user_id' %>
  <% end %>
  to enter the room.
</div>
